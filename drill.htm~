













<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
		<title>Receitas</title>
		
	<link href="bootstrap.min.css" rel="stylesheet">
        <!-- TWITTER BOOTSTRAP CSS -->
        <link href="./bootstrap/css/bootstrap.css" rel="stylesheet" type="text/css" />
 	<link href="./bootstrap/css/bootstrap-responsive.min.css" rel="stylesheet">
        <!-- TWITTER BOOTSTRAP JS -->
        <script src="./bootstrap/js/bootstrap.min.js"></script>

		<!Incluindo JQuery>
		<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"></script>
		<script type="text/javascript" src="./js/highcharts.js"></script>
	

<script type="text/javascript">

(function($){ // encapsulate jQuery

$(function () {
    var chart;
    $(document).ready(function() {
    
        var colors = Highcharts.getOptions().colors,
            categories = ['RECEITAS CORRENTES', 'RECEITAS DE CAPITAL'],
            name = 'Receitas Previstas',
            data = [{
                    y: 16821410340.0,
                    color: colors[0],
                    drilldown: {
                        name: 'RECEITAS CORRENTES',
                        categories: ['RECEITA TRIBUTÁRIA', 'TRANSFERÊNCIAS CORRENTES', 'RECEITA DE CONTRIBUIÇÕES'],
                        data: [10.85, 7.35, 33.06],
                        color: colors[0]
                    }
                }, {
                    y: 1794068641.0,
                    color: colors[1],
                    drilldown: {
                        name: 'RECEITAS DE CAPITAL',
                        categories: ['TRANSFERÊNCIAS DE CAPITAL'],
                        data: [0.20],
                        color: colors[1]
                    }
                }
                }];
    
        function setChart(name, categories, data, color) {
			chart.xAxis[0].setCategories(categories, false);
			chart.series[0].remove(false);
			chart.addSeries({
				name: name,
				data: data,
				color: color || 'white'
			}, false);
			chart.redraw();
        }
    
        chart = new Highcharts.Chart({
            chart: {
                renderTo: 'container',
                type: 'column'
            },
            title: {
                text: 'Browser market share, April, 2011'
            },
            subtitle: {
                text: 'Click the columns to view versions. Click again to view brands.'
            },
            xAxis: {
                categories: categories
            },
            yAxis: {
                title: {
                    text: 'Total percent market share'
                }
            },
            plotOptions: {
                column: {
                    cursor: 'pointer',
                    point: {
                        events: {
                            click: function() {
                                var drilldown = this.drilldown;
                                if (drilldown) { // drill down
                                    setChart(drilldown.name, drilldown.categories, drilldown.data, drilldown.color);
                                } else { // restore
                                    setChart(name, categories, data);
                                }
                            }
                        }
                    }
                }
            },
            tooltip: {
                formatter: function() {
                    var point = this.point,
                        s = this.x +':<b>'+ this.y +'% market share</b><br/>';
                    if (point.drilldown) {
                        s += 'Click to view '+ point.category +' versions';
                    } else {
                        s += 'Click to return to browser brands';
                    }
                    return s;
                }
            },
            series: [{
                name: name,
                data: data,
                color: 'white'
            }],
            exporting: {
                enabled: true
            }
        });
    });
    
});
})(jQuery);
</script>






	</head>
	<body>


        <!-- HEADER -->
        <header class="container-fluid">
            <div class="row">
                <div class="span12">
                    <div class="navbar">
                        <div class="navbar-inner">
                            <div class="container">
                                <h2>Lucidy Scrap</h2>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </header>
        <!-- / HEADER -->





<!-- Masthead
================================================== -->
<header class="jumbotron subhead" id="overview">
  <div class="row">
    <div class="span10 offset1">
<!--      <h1>LucidyScrapy</h1>-->
      <p class="lead">This is a project about government open data that uses Python to get information about expenses from Governo do Distrito Federal (Brazil). </p>
    </div>
     </div>



  <div class="subnav">
    <ul class="nav nav-pills">
      <li><a href="#">Receitas</a></li>
    </ul>
  </div>
</header>



		<script src="./js/modules/exporting.js"></script>
		<!-- 3. Add the container -->
		<div id="container" style="width: 800px; height: 400px; margin: 0 auto"></div>
		<script type="text/javascript" src="./js/themes/gray.js"></script>
	</body>
</html>
